# Java-CricketScoreboard
A simple Java program demonstrating the use of LinkedHashMap to store and manage cricket players’ scores. The project showcases how to: - Insert player names with scores into a map - Retrieve values by key (player name) - Display all keys (player names) and values (scores) - Iterate through the map using forEach and keySet .
